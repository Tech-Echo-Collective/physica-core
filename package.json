{
  "name": "physica-core-validation",
  "version": "0.7.0",
  "private": true,
  "description": "Validation tooling for the language-neutral Tech Echo Physica ontology",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:registry": "node scripts/validate.mjs --write-registry",
    "validate": "node scripts/validate.mjs",
    "test": "npm run validate"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
